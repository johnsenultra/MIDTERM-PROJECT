

// inputValidation.post("/registerUser", zValidator("json", registrationSchema), async (c) => {
//    const { password } = await c.req.json();
   
//    // Hash the password
//    const hashedPassword = await bcrypt.hash("uniquePass", 10);

//    const comparePassowrd = await bcrypt.compare(password, hashedPassword);


// });